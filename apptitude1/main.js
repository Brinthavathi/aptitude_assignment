var bucket=prompt("enter a bucket");
var capacityTank=prompt("enter a capacityTank");
bucket=parseInt(bucket);
capacityTank=parseFloat(capacityTank);
var nbucket=bucket/capacityTank;
document.write(nbucket);